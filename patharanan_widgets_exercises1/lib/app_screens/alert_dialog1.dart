/* import 'package:flutter/material.dart';

class AlertDialog extends StatelessWidget {
  const AlertDialog({Key? key}) : super(key: key);


@override
Widget build(BuildContext context){
  return Scaffold(
     => showDialog<String>(context: context, builder: (BuildContext context)=>AlertDialog(title:const Text("Confirmation"),content: Text("Submitting information"),actions: [
        <Widget>[
          TextButton(onPressed: Navigator.pop(context,"Cancle"), child: const Text("Cancle"),),
          TextButton(onPressed: Navigator.pop(context,"OK"), child: const Text("OK"))
        ]
      ],)){
        
      },
      child: const Text("Submit"),
    );
  }
  )
}
}

 */